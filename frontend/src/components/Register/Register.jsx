import React from 'react'

const Register = () => {
  return (
    <div>
        <form>
            <input type="email" />
            <input type="password" />
            <button>Đăng ký</button>
        </form>
    </div>
  )
}

export default Register
