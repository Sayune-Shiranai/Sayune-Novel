import React from 'react'

const Login = () => {
  return (
    <div>
        <form>
            <input type="email" />
            <input type="password" />
            <button>Đăng nhập</button>
        </form>
    </div>
  )
}

export default Login
